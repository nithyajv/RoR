<!DOCTYPE html>
<html>
  <head>
    <title>Tasklist1</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>


<p class="navbar-text float-right">
<% if user_signed_in? %>
  Logged in as <strong><%= current_user.email %></strong>.

  <%= link_to t('link.edit_profile'), edit_user_registration_path, :class => 'navbar-link' %>
  <%= link_to t('link.user_list'), users_path,class:"navbar-link" %>
  <%= link_to t('link.task_list'), tasklists_path,class:"navbar-link" %>
  <%= link_to t('link.new_task'), new_tasklist_path,class:"navbar-link" %>
  <%= button_to t('link.sign_out'), destroy_user_session_path, method: :delete ,class:"navbar-link" %>

<% else %>
  <%= link_to  t('link.sign_up'), new_user_registration_path, :class => 'navbar-link'  %> |
  <%= link_to t('link.sign_in'), new_user_session_path, :class => 'navbar-link'  %>

<% end %>
</p>

  <p style="color: green" class="alert alert-success"><%= notice %></p> 
<!---
  <% if alert %>
   <p class="alert alert-danger">
   <%= alert %></p> 
   <% end %>
-->
      <%= yield %>
      <div>

      <br>
          <%= link_to "English", root_path(locale: :en)%> |
          <%= link_to "русский", root_path(locale: :ru)%>
      <br>
      </div>

  </body>
</html>
